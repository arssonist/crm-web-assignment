<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>CRM web assignment</title>
  </head>
  <body>
    <h1>All Contacts</h1>
    <p>
      <ul>
        <% Contact.all.each do |contact| %>
        <li><%= contact.first_name %> <%= contact.last_name %></li>
        <% end %>
    </ul>
    </p>
  </body>
</html>
